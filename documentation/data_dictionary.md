# Data Dictionary

*Status: placeholder — populated when fact/dimension tables are defined and loaded.*

For each field, document: name, table, data type, definition, metric label (reported / calculated / estimated / modeled), source Source_ID(s), and null policy.
