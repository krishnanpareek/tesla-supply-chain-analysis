# Environment and secrets — never commit credentials
.env
.env.*
!.env.example
*.pem
*.key
*credentials*
*secret*
*apikey*
*api_key*
*token*.json

# Power BI backups
*.pbix.bak

# OS / editor junk
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*~

# Python / notebook artifacts (if used later)
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# Large raw source files — keep READMEs/.gitkeep; document source URLs instead of committing multi-MB downloads
data/raw/**/*.csv
data/raw/**/*.tsv
data/raw/**/*.xlsx
data/raw/**/*.xls
data/raw/**/*.zip
data/raw/**/*.gz
data/raw/**/*.json
data/raw/**/*.jsonl
data/raw/**/*.parquet
data/raw/**/*.xml
data/raw/**/*.pdf

# Multi-MB SEC primary filings (re-download via data/reference/download_log.md)
data/raw/sec_edgar/**/*.htm
data/raw/sec_edgar/**/*.html

# Transient download helper artifacts
data/reference/_tmp_*.json
data/reference/_sec_*.json
data/reference/_sec_*.atom

# Allow small placeholder / inventory companions under raw
!data/raw/**/README.md
!data/raw/**/.gitkeep
